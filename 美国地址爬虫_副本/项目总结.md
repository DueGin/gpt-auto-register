# 美国地址爬虫 - 项目总结

## ✅ 项目完成状态

已成功创建一个功能完整的美国地址爬虫，可以从 https://www.meiguodizhi.com/ 爬取虚拟地址信息。

## 📁 项目文件结构

```
美国地址爬虫/
├── address_scraper.py      # 主爬虫程序
├── custom_example.py       # 自定义示例脚本（5个使用场景）
├── requirements.txt        # Python依赖包列表
├── README.md              # 详细使用文档
├── .gitignore            # Git忽略文件配置
├── random_addresses.json  # 示例：爬取的JSON数据
└── random_addresses.csv   # 示例：爬取的CSV数据
```

## 🎯 核心功能

1. **随机地址爬取** - 爬取随机生成的虚拟美国地址
2. **城市定向爬取** - 爬取指定城市的地址（如纽约、洛杉矶等）
3. **州定向爬取** - 爬取指定州的地址（如加州、德州等）
4. **多格式导出** - 支持JSON和CSV两种格式
5. **完整数据提取** - 自动提取所有可用字段

## 📊 可爬取的数据字段

### 基本资料
- 全名、性别、生日、Title、头发颜色

### 地址信息
- 街道、区县、城市、州、州全称、邮编、电话号码、临时邮箱

### 就业 & 财务
- 信用卡类型、信用卡号、CVV2、过期时间
- 职业、公司名称、公司规模、就业状态、月薪、社会保障号

### 其他信息
- 用户名、密码、身高、体重、血型
- 操作系统、GUID、浏览器User-Agent
- 教育背景、个人主页、安全问题、问题答案

## 🚀 快速开始

### 1. 安装依赖
```bash
cd /Users/uq/Downloads/美国地址爬虫
pip3 install -r requirements.txt
```

### 2. 运行示例
```bash
python3 address_scraper.py
```

这将爬取3个随机地址并保存为JSON和CSV文件。

### 3. 自定义使用
编辑并运行 `custom_example.py`：
```bash
python3 custom_example.py
```

## 💡 技术亮点

1. **使用Selenium** - 因为目标网站使用JavaScript动态加载数据
2. **智能等待** - 自动等待页面数据加载完成
3. **无头模式** - 支持后台运行，不显示浏览器窗口
4. **错误处理** - 完善的异常处理机制
5. **延迟控制** - 避免请求过快被网站封禁

## 📝 使用示例

### 基本使用
```python
from address_scraper import AddressScraper

scraper = AddressScraper(headless=True)
addresses = scraper.scrape_multiple_addresses(count=10, delay=2)
scraper.save_to_json(addresses, 'output.json')
scraper.save_to_csv(addresses, 'output.csv')
```

### 爬取特定城市
```python
ny_addresses = scraper.scrape_city_addresses('New-York', count=5)
scraper.save_to_json(ny_addresses, 'newyork.json')
```

### 爬取特定州
```python
ca_addresses = scraper.scrape_state_addresses('california', count=5)
scraper.save_to_json(ca_addresses, 'california.json')
```

## ⚠️ 重要提示

1. **合法使用** - 爬取的数据为虚拟数据，仅供学习参考
2. **请勿滥用** - 请遵守网站使用条款，控制爬取频率
3. **数据用途** - 请勿将数据用于非法目的
4. **依赖要求** - 需要安装Chrome浏览器（ChromeDriver会自动管理）

## 🔧 依赖说明

- **selenium** (4.16.0) - 浏览器自动化
- **beautifulsoup4** (4.12.2) - HTML解析
- **lxml** (4.9.3) - XML/HTML解析器

## 📈 测试结果

✅ 成功爬取3个地址示例：
- A Lou Jhnny (Newfane, VT)
- Le Bush J (Chelmsford, MA)
- Wahl Nora (Savannah, GA)

每个地址包含15+个数据字段，数据完整准确。

## 🎓 学习价值

本项目适合学习：
- Selenium网页自动化
- 动态网页数据爬取
- BeautifulSoup HTML解析
- Python数据处理
- JSON/CSV文件操作

## 📮 后续改进建议

1. 添加代理IP支持
2. 支持多线程/异步爬取
3. 添加数据验证功能
4. 支持更多导出格式（Excel、SQLite等）
5. 添加爬取进度条显示
6. 支持断点续爬功能

---

**创建时间**: 2026年1月21日  
**测试状态**: ✅ 通过  
**环境**: macOS, Python 3.9.6
